# Deny everything.
**

!/LICENSE
!/docker-entrypoint.sh
!/replace-placeholders-in-sql-schema-migrations.sh

!/hasura/**
# /hasura/** covers the following files so ignore them explicitly.
/hasura/.env
/hasura/config.yaml
